# Homebrew Tap for TAPD CLI

Install `tapd` with Homebrew:

```bash
brew install --cask go-tapd/tap/tapd
```

The cask is generated by GoReleaser from
[`go-tapd/cli`](https://github.com/go-tapd/cli) releases.
